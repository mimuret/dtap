{
  "$id": "/pkg/plugin/input/nats/config-schema.json",
  "title": "nats input plugin",
  "type": "object",
  "required": [
    "Name",
    "ID",
    "Hosts",
    "Subject"
  ],
  "properties": {
    "Name": {
      "const": "nats"
    },
    "ID": {
      "type": "string"
    },
    "Hosts": {
      "description": "The `Hosts` are hosts:port to connect nats server.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "Subject": {
      "description": "The `Subject` is the nats message subject.",
      "type": "string"
    },
    "QueueName": {
      "description": "The `QueueName` is the nats queue subscription name.",
      "type": "string"
    },
    "Token": {
      "description": "The `Token` is the nats Token for authentication.",
      "type": "string"
    },
    "User": {
      "description": "The `User` is the nats user for authentication.",
      "type": "string"
    },
    "Password": {
      "description": "The `User` is the nats password for authentication.",
      "type": "string"
    },
    "Format": {
      "$ref": "https://github.com/mimuret/dtap/raw/refs/heads/v2/schemas/types.json#/$defs/OutputBinFormat"
    }
  }
}