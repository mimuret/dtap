{
  "$id": "/pkg/plugin/output/nats/config-schema.json",
  "title": "nats output plugin",
  "type": "object",
  "required": [
    "Name",
    "Hosts",
    "Subject"
  ],
  "properties": {
    "Name": {
      "const": "nats"
    },
    "MaxSize": {
      "description": "The `MaxSize` is the nats max payload size.",
      "type": "integer",
      "minimum": 0,
      "default": 1048576
    },
    "Hosts": {
      "description": "The `Hosts` are hosts:port to connect nats server.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      }
    },
    "Subject": {
      "description": "The `Subject` is the nats message subject.",
      "type": "string"
    },
    "Token": {
      "description": "The `Token` is the nats Token for authentication.",
      "type": "string"
    },
    "User": {
      "description": "The `User` is the nats user for authentication.",
      "type": "string"
    },
    "Password": {
      "description": "The `User` is the nats password for authentication.",
      "type": "string"
    }
  }
}