{
  "$id": "/pkg/plugin/output/file/config-schema.json",
  "title": "DNS output plugin",
  "type": "object",
  "required": [
    "Host"
  ],
  "properties": {
    "Name": {
      "const": "dns"
    },
    "Host": {
      "type": "string",
      "description": "The host and port of the DNS server to send logs to.",
      "example": "192.178.0.1:53"
    },
    "Timeout": {
      "type": "string",
      "description": "The timeout for sending logs to the DNS server.",
      "default": "1s"
    },
    "HTTPConfig": {
      "type": "object",
      "properties": {
        "Path": {
          "type": "string",
          "description": "The HTTP Path to send logs to.",
          "default": "/dns-query"
        },
        "Method": {
          "type": "string",
          "description": "The HTTP Method to use.",
          "enum": [
            "POST",
            "GET"
          ],
          "default": "POST"
        },
        "TLS": {
          "type": "bool",
          "description": "Whether to use TLS or not.",
          "default": true
        }
      }
    },
    "Protocol": {
      "type": "string",
      "enum": [
        "udp",
        "tcp",
        "tls",
        "https"
      ],
      "default": "udp"
    }
  }
}